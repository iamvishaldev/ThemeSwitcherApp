import React from "react";
import ThemeToggler from "../Components/ThemeToggler";

const Header = () => {
  return (
    <header>
      <h1>theme toggler</h1>
      <ThemeToggler />
    </header>
  );
};

export default Header;
